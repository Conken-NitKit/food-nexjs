import { Test, TestingModule } from '@nestjs/testing';
import { {{pascalCase name}}Controller } from './{{name}}.controller';
import { {{pascalCase name}}Service } from './{{name}}.service';

describe('{{pascalCase name}}Controller', () => {
  let {{name}}Controller: {{pascalCase name}}Service;

  beforeEach(async () => {
    const {{name}}: TestingModule = await Test.createTestingModule({
      controllers: [{{pascalCase name}}Controller],
      providers: [{{pascalCase name}}Service],
    }).compile();

    {{name}}Controller = {{name}}.get<{{pascalCase name}}Service>({{pascalCase name}}Controller);
  });

  describe('root', () => {
    it('should return "Hello {{pascalCase name}}!"', () => {
      expect({{name}}Controller.getHello()).toBe('Hello {{pascalCase name}}!');
    });
  });
});
